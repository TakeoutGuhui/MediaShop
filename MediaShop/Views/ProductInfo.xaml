<Window x:Class="MediaShop.Views.ProductInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Name}" Height="300" Width="300"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Label Content="Total sold:" />
            <Label Content="{Binding TotalItemsSold}" />
            <Label Content="Total income:" />
            <Label Content="{Binding TotalIncome}" />
            
        </StackPanel>
        
        <Grid>
            <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding Sales}" CanUserAddRows="False" />
        </Grid>
    </DockPanel>
</Window>
