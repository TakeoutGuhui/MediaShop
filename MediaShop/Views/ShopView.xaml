<UserControl x:Class="MediaShop.Views.ShopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaShop.Views"
             xmlns:datacontext="clr-namespace:MediaShop.ViewModels"
             xmlns:converters="clr-namespace:MediaShop.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=datacontext:ShopViewModel}">
    <UserControl.Resources>
        <converters:PriceLabelConverter x:Key="PriceLabelConverter" />
    </UserControl.Resources>
    
    <DockPanel LastChildFill="True">
           <ListView Width="300" DockPanel.Dock="Right" ItemsSource="{Binding ShoppingCart.CartItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                        <DockPanel LastChildFill="True">
                            <Label DockPanel.Dock="Left" Content="{Binding Product.Name}"/>
                            
                            <Label DockPanel.Dock="Right" Content="{Binding TotalPrice}"/>
                            <Label HorizontalContentAlignment="Center">
                                <Label.Content>
                                    <MultiBinding  Converter="{StaticResource PriceLabelConverter}">
                                        <Binding Path="NumItemsInCart"/>
                                        <Binding Path="Product.Price"/>
                                    </MultiBinding>
                                </Label.Content>
                            </Label>
                    </DockPanel>
                </DataTemplate>
                </ListView.ItemTemplate>
               <ListView.ItemContainerStyle>
                   <Style TargetType="ListViewItem">
                       <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                   </Style>
               </ListView.ItemContainerStyle>
        </ListView> 
           <ListView ItemsSource="{Binding ProductList.Products}" SelectedItem="{Binding SelectedProduct}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" >
                        <StackPanel.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.AddToCartCommand}" />
                        </StackPanel.InputBindings>
                        <Label Content="{Binding Name}"/>
                        <Label Content="{Binding Stock}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>
